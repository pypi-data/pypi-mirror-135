from setuptools import setup

setup(name='distributions_test_example',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_test_example'],
      zip_safe=False)

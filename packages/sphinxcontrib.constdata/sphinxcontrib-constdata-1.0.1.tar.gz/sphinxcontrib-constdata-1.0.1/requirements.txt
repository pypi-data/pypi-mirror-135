# pbr will use this list as install_requires for PyPI package
# list runtime and distribution dependencies only
# (formatters, mypy etc. goes to dev-requirements.txt)
pbr==5.8.0
Sphinx>=4.2.0
pyaml==21.10.1